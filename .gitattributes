# Windows batch files must keep CRLF line endings
*.bat text eol=crlf
*.cmd text eol=crlf

# Shell scripts must keep LF line endings
*.sh text eol=lf

# Python files — cross-platform, let Git decide
*.py text

# Auto-detect text files
* text=auto