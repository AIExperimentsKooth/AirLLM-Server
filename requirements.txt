# AirLLM OpenAI-Compatible Server - Dependencies (cross-platform)
# Install via install.sh (Linux/macOS) or install.bat (Windows).
# These scripts install PyTorch separately with the right CUDA support.

# Core: AirLLM with CPU/GPU layer streaming
airllm>=3.0.0

# Web framework for OpenAI-compatible API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Qwen3.8-27B needs transformers 5.8+
transformers>=5.8.0

# Tokenization helpers
sentencepiece>=0.2.0

# Utilities
pydantic>=2.0.0
requests>=2.31.0

# Model compression: 4-bit/8-bit quantization (auto-enabled for models >= 7B)
# On Windows: pip install bitsandbytes-windows (see install.bat)
bitsandbytes>=0.43.0